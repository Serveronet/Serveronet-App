<?php

namespace App\Dicts;

class DataTypes
{
    const site_definitions = 'site_definitions';
    
    const site_peers = 'site_peers';

    const peers = 'peers';

    const visitor_records = 'visitor_records';

    const visitor_resources = 'visitor_resources';

    const cached_resources = 'cached_resources';
    
    const trackers = 'trackers';

    const visitors = 'visitors';
    
    const internal_requests = 'internal_requests';

    const local_tracker_info_hash_peers = 'local_tracker_info_hash_peers';

    const replication_sessions = 'replication_sessions';
    
    const peer_replication_sessions = 'peer_replication_sessions';
}
